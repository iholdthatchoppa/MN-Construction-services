import ProjectGallery from '../components/ProjectGallery';

const Projects = () => {
  return (
    <section style={{ padding: '2rem', textAlign: 'center' }}>
      <h1>Our Projects</h1>
      <p>Take a look at some of the stunning projects we've completed.</p>
      <ProjectGallery />
    </section>
  );
};

export default Projects;
